package com.dungochung.shopdongho.enums;

/** Loại biến động tồn kho ghi trong inventory_movements. */
public enum MovementType {
	INITIAL, PURCHASE_IN, PURCHASE_REVERSAL, ADJUSTMENT, ORDER_RESERVE, ORDER_RELEASE, ORDER_SHIPPED, ORDER_RETURN
}
